# Database

Prisma targets PostgreSQL through `DATABASE_URL`. Run `npm run db:generate`, `npm run db:migrate`, and `npm run db:seed`.

Seeded records are fictional demonstration data. They must not be presented as live vacancies or legal advice.
