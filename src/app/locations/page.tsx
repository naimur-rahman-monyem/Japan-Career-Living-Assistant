import { db } from "@/lib/db"; import Link from "next/link";
export const dynamic="force-dynamic";
export default async function Locations(){const rows=await db.location.findMany({include:{costOfLiving:true,_count:{select:{jobs:true}}}});return <main className="container py-16"><p className="font-semibold text-ocean">FIND YOUR PLACE</p><h1 className="mt-3 text-4xl font-bold">Cities with room for your next chapter</h1><div className="mt-10 grid gap-5 md:grid-cols-3">{rows.map(l=><Link href={`/jobs?location=${l.slug}`} className="card p-6" key={l.id}><h2 className="text-2xl font-bold">{l.name}</h2><p className="mt-1 text-sm text-slate-500">{l.prefecture}</p><p className="mt-4 leading-7 text-slate-600">{l.description}</p><p className="mt-6 text-sm font-semibold">Typical rent: ¥{l.costOfLiving?.monthlyRent.toLocaleString()} · {l._count.jobs} roles</p></Link>)}</div></main>}
