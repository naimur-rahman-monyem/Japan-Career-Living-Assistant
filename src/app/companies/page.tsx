import { db } from "@/lib/db"; import Link from "next/link";
export const dynamic="force-dynamic";
export default async function Companies(){const rows=await db.company.findMany({include:{_count:{select:{jobs:true}}}});return <main className="container py-16"><p className="font-semibold text-ocean">EMPLOYERS</p><h1 className="mt-3 text-4xl font-bold">Companies building in Japan</h1><div className="mt-10 grid gap-5 md:grid-cols-3">{rows.map(c=><Link href={`/jobs?company=${c.slug}`} className="card p-6" key={c.id}><p className="text-xs font-bold uppercase text-ocean">{c.industry}</p><h2 className="mt-4 text-xl font-bold">{c.name}</h2><p className="mt-3 text-sm leading-6 text-slate-600">{c.description}</p><p className="mt-6 text-sm font-semibold text-ocean">{c._count.jobs} open roles →</p></Link>)}</div></main>}
