import { db } from "@/lib/db";
export const dynamic="force-dynamic";
export default async function Visa(){const rows=await db.visaInformation.findMany();return <main className="container py-16"><p className="font-semibold text-ocean">MAKE IT OFFICIAL</p><h1 className="mt-3 text-4xl font-bold">Visa pathways, made clearer</h1><p className="mt-4 text-slate-600">Always verify requirements with Japan’s official immigration authority.</p><div className="mt-10 grid gap-5 md:grid-cols-2">{rows.map(v=><article className="card p-6" key={v.id}><h2 className="text-xl font-bold">{v.name}</h2><p className="mt-3 leading-7 text-slate-600">{v.summary}</p><p className="mt-4 text-sm"><b>Typical duration:</b> {v.typicalDuration}</p><a href={v.officialUrl||"#"} className="mt-6 inline-block text-sm font-bold text-ocean">Official guidance →</a></article>)}</div></main>}
